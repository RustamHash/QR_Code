# QR Code Generator Bot

Telegram-бот для генерации QR-кодов из данных Excel файлов и текстовых сообщений. Бот принимает Excel файлы (.xlsx, .xls) или текстовые сообщения, читает данные и создает PDF файл с QR-кодами.

## Возможности

- 📤 Прием Excel файлов (.xlsx, .xls)
- 📝 Обработка текстовых сообщений (одна строка или несколько)
- 🔲 Автоматическая генерация QR-кодов
- 📄 Создание PDF файла с QR-кодами
- ⚙️ Настройка параметров PDF (ширина, высота, количество строк и колонок на странице)
- 💾 Сохранение настроек пользователя между сессиями
- 🎯 Интерактивное меню настроек с inline-кнопками
- 📊 История обработки файлов
- 📈 Статистика для администратора
- 📝 Подробное логирование всех операций
- 🔒 Rate limiting для защиты от злоупотреблений
- 🐳 Docker контейнеризация

## Установка

### Требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. Клонируйте репозиторий или скачайте проект:
```bash
git clone <repository_url>
cd QR_Code
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:

   **Windows:**
   ```bash
   venv\Scripts\activate
   ```

   **Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

4. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Настройка

### Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания нового бота
4. Скопируйте полученный токен

### Настройка конфигурации

1. Скопируйте файл `.env.example` в `.env`:
```bash
copy .env.example .env
```

2. Откройте файл `.env` и укажите:
   - `TELEGRAM_BOT_TOKEN` - токен вашего бота
   - `ADMIN_ID` - ваш Telegram User ID (опционально, для уведомлений)

Для получения своего Telegram ID используйте бота [@userinfobot](https://t.me/userinfobot).

## Запуск

### Локальный запуск

```bash
python main.py
```

### Запуск в Docker

#### Локальная разработка

1. Создайте файл `.env` (см. выше)

2. Соберите образ:
```bash
docker-compose -f docker-compose_local.yml build
```

3. Запустите контейнер:
```bash
docker-compose -f docker-compose_local.yml up -d
```

4. Просмотр логов:
```bash
docker-compose -f docker-compose_local.yml logs -f
```

#### Развертывание на Timeweb Cloud

Файл `docker-compose.yml` настроен для Timeweb Cloud (без volumes).

**Важно:** После создания приложения через MCP сервер или вручную, настройте переменные окружения в панели Timeweb Cloud:
- `TELEGRAM_BOT_TOKEN` - токен вашего бота
- `ADMIN_ID` - ваш Telegram ID (опционально)
- `DATABASE_URL=sqlite:///app/bot_database.db`
- `LOG_LEVEL=INFO`
- `LOG_FILE=bot.log`

## Использование

### Основные команды

- `/start` - Начать работу с ботом и посмотреть текущие настройки
- `/help` - Показать справку по использованию бота
- `/settings` - Открыть интерактивное меню настроек
- `/width <значение>` - Установить ширину страницы в мм (по умолчанию: 75)
- `/height <значение>` - Установить высоту страницы в мм (по умолчанию: 120)
- `/rows <значение>` - Установить количество строк на странице (по умолчанию: 5)
- `/columns <значение>` - Установить количество колонок на странице (по умолчанию: 1)
- `/reset` - Сбросить настройки к значениям по умолчанию
- `/history` - Просмотреть историю обработки
- `/stats` - Статистика (только для администратора)

### Работа с файлами

1. Отправьте боту Excel файл (.xlsx или .xls)
2. Бот автоматически прочитает данные из **первой колонки первого листа**, начиная с **первой строки** (без заголовка)
3. Бот сгенерирует PDF файл с QR-кодами и отправит его вам

### Работа с текстом

1. Отправьте боту текстовое сообщение:
   - **Одна строка** = один QR-код
   - **Несколько строк** (разделенных Enter) = несколько QR-кодов
2. Бот автоматически определит формат и создаст PDF с QR-кодами

### Формат Excel файла

- Файл должен быть в формате `.xlsx` или `.xls`
- Данные должны находиться в **первой колонке** (колонка A)
- Чтение начинается с **первой строки** (заголовок не требуется)
- Пустые ячейки будут пропущены

**Пример структуры Excel файла:**
```
A
1  Данные для QR-кода 1
2  Данные для QR-кода 2
3  Данные для QR-кода 3
4  Данные для QR-кода 4
```

### Настройки PDF

Вы можете настроить следующие параметры PDF файла:

- **Ширина страницы** (по умолчанию: 75 мм)
- **Высота страницы** (по умолчанию: 120 мм)
- **Количество строк на странице** (по умолчанию: 5)
- **Количество колонок на странице** (по умолчанию: 1)

Настройки можно изменить:
- Через интерактивное меню `/settings`
- Через команды `/width`, `/height`, `/rows`, `/columns`

**Важно:** Настройки сохраняются для каждого пользователя отдельно и восстанавливаются после перезапуска бота.

## Структура проекта

```
QR_Code/
├── src/
│   ├── bot/
│   │   ├── handlers/      # Обработчики команд и сообщений
│   │   ├── keyboards/     # Клавиатуры
│   │   └── middleware/     # Middleware (rate limiting)
│   ├── core/              # Ядро приложения
│   │   ├── config.py      # Конфигурация
│   │   ├── logging_config.py  # Логирование
│   │   └── exceptions.py  # Исключения
│   ├── services/          # Бизнес-логика
│   │   ├── qr_service.py  # Генерация QR-кодов
│   │   ├── excel_service.py  # Обработка Excel
│   │   ├── pdf_service.py  # Создание PDF
│   │   ├── file_service.py # Работа с файлами
│   │   └── text_service.py # Обработка текста
│   ├── database/          # Работа с БД
│   │   ├── models.py      # Модели SQLAlchemy
│   │   ├── database.py    # Инициализация БД
│   │   └── repositories.py # Репозитории
│   └── utils/             # Утилиты
│       ├── validators.py  # Валидация
│       └── helpers.py     # Вспомогательные функции
├── tests/                 # Тесты
├── alembic/               # Миграции БД
├── main.py               # Точка входа
├── requirements.txt      # Зависимости
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose
├── .env.example          # Пример конфигурации
└── README.md             # Документация
```

## Зависимости

- `python-telegram-bot` - Библиотека для работы с Telegram Bot API
- `pydantic` + `pydantic-settings` - Валидация конфигурации
- `sqlalchemy` - ORM для работы с базой данных
- `alembic` - Миграции базы данных
- `pandas` - Обработка Excel файлов
- `openpyxl` - Поддержка формата .xlsx
- `xlrd` - Поддержка формата .xls
- `qrcode[pil]` - Генерация QR-кодов
- `fpdf2` - Создание PDF файлов

## Логирование

Все операции бота логируются в файл `bot.log` и выводятся в консоль. Логи включают:

- Команды пользователей
- Обработку файлов и текста
- Изменения настроек
- Ошибки и исключения

## Разработка

### Запуск тестов

```bash
pytest
```

### Линтинг кода

```bash
black src/
flake8 src/
mypy src/
```

### Миграции базы данных

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Описание изменений"

# Применить миграции
alembic upgrade head
```

## Разрешение проблем

### Бот не запускается

- Проверьте, что токен бота указан корректно в `.env`
- Убедитесь, что установлены все зависимости
- Проверьте логи в файле `bot.log`

### Файлы не обрабатываются

- Убедитесь, что файл имеет расширение `.xlsx` или `.xls`
- Проверьте, что данные находятся в первой колонке
- Проверьте логи для деталей ошибки

### Настройки не сохраняются

- Убедитесь, что бот имеет права на запись в директорию проекта
- Проверьте базу данных `bot_database.db`

## Лицензия

Этот проект предоставляется "как есть" без каких-либо гарантий.

## Автор

Проект создан для автоматизации генерации QR-кодов из данных Excel файлов и текстовых сообщений.

